on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main

    
jobs:
  my-job:
    name: My Job
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Print a greeting
        run: make hello 
